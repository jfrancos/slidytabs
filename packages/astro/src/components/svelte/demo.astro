---
import { Code } from "astro:components";
import { transformerMetaWordHighlight } from "@shikijs/transformers";
import Shell from "./Shell.svelte";

const { filename, highlight } = Astro.props;
const { default: code } = await import(`./examples/${filename}.svelte?raw`);
---

<div class="flex flex-col border-gray-100 border-4 rounded-lg w-xl">
  <div class="p-8 flex justify-center">
    <Shell {filename} client:load />
  </div>
  <Code
    theme="github-light"
    lang="svelte"
    class="text-sm px-6 py-5 !bg-neutral-100"
    transformers={[transformerMetaWordHighlight()]}
    meta={`/${highlight}/`}
    {code}
  />
</div>
