---
import { codeToHtml, type BundledLanguage } from "shiki";

interface Props {
  code: string;
  highlight?: { start: number; end: number }[];
  lang: BundledLanguage;
  class?: string;
}
const { code, highlight, lang, class: className } = Astro.props;
const html = codeToHtml(code, {
  lang,
  theme: "github-light",
  decorations: highlight?.map(({ start, end }) => ({
    start,
    end,
    properties: { class: "highlight" },
  })),
});
---

<div
  set:html={html}
  class=`children:(text-sm px-6 py-5 !bg-neutral-100 overflow-scroll) ${className}`
/>
