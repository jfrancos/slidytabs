---
import { getCollection } from "astro:content";
import "github-markdown-css/github-markdown.css";

interface Props {
  section: string;
}

const { section } = Astro.props;

const readme = await getCollection("readme");
const sections = Object.fromEntries(
  readme.map(({ id, rendered }) => [id, rendered?.html])
);
---

<!-- <article
  class=".my-0 prose prose-neutral max-w-none prose-h2:(border-0 pb-2 border-b border-gray-300! mt-6! mb-4!) prose-pre:bg-#f6f8fa! [&_code:not(pre>code)]:(bg-#818b981f px-1 -mx-0.5 py-0.5 -my-0.25 rounded font-medium) prose-code:before:content-none prose-code:after:content-none"
> --><!-- <article class="markdown-body"> -->
<div class="markdown-body" set:html={sections[section]} />
<!-- </article> -->
